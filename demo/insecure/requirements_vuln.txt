# DEMO-ФИКСТУРА для G2 (pip-audit) и G3 (typosquatting). НЕ устанавливать в прод.
# Старые версии с известными CVE — чтобы показать красную сборку (#8):
flask==1.0.2
numpy==1.21.0
# Опечатки-двойники для G3 (#9) — должны блокироваться allow-list:
pytirch
tenserflew
