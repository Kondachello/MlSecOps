# Архитектура ML-платформы

## Описание системы

Платформа обеспечивает полный цикл разработки и деплоя ML-моделей: от загрузки данных и кода датасайнтистами — через версионирование в MLflow — до автоматизированного CI/CD пайплайна с security-проверками и деплоем на прод.

---

## Граф архитектуры

```mermaid
flowchart TD
    %% ─── Входные артефакты ───
    subgraph INPUTS["📥 ВХОДЫ (через бэк)"]
        CODE["💻 Код"]
        MODEL["🤖 Модель"]
        DATA["📊 Данные"]
    end

    DS["👨‍💻 Датасайнтист\nРазработка данных,\nмоделей, кода"]
    FRONT["🖥️ Веб-интерфейс / Фронт"]

    %% ─── Зелёный контур: единый аккаунт ───
    subgraph SHARED_ACCOUNT["🟢 Один акк на наш сервис и на mlflow"]
        BEK["🔧 Бэк"]
        MLFLOW["📦 MLflow\n(добавить регистрацию)"]
        CI_SEC["🔒 CI/CD\nSecurity Check"]
        DB_SVC["🗄️ БД нашего сервиса"]
    end

    %% ─── Хранилище S3 ───
    subgraph S3["☁️ S3: модели, данные, код"]
        DUMP["🗑️ Свалка"]
        FAIL["❌ Не прошли проверку"]
        OK["✅ OK"]
        PROD_STORE["🚀 !ПРОД!"]
    end

    %% ─── Прод-деплой и мониторинг ───
    CI_PROD["⚙️ CI/CD прода"]
    MONITOR["📡 Мониторинг\nСледим за API и метриками:\nзащита от DDoS, контроль\nутечки моделей и т.д."]
    DB_ML["💾 База данных MLflow\n(видимо, автоматически\nподдерживается)"]

    %% ─── Связи ───
    INPUTS -->|"Загрузка через бэк"| FRONT
    FRONT --> BEK
    DS -->|"Разработка и загрузка\nданных/моделей через MLflow"| MLFLOW

    BEK <-->|"в бэке отображаются\nвсе загрузки в MLflow"| MLFLOW
    BEK --- DB_SVC
    BEK --> CI_PROD
    BEK --> CI_SEC

    CI_SEC -->|"security check"| MLFLOW
    MLFLOW --> CI_SEC

    MLFLOW --> DB_ML
    MLFLOW -->|"свалка"| DUMP
    MLFLOW -->|"не прошли проверку"| FAIL
    MLFLOW -->|"ок"| OK
    MLFLOW -->|"деплой"| PROD_STORE

    CI_PROD --> MONITOR
    CI_PROD --> PROD_STORE

    %% ─── Стили ───
    classDef input      fill:#EEF2FF,stroke:#6366F1,color:#1e1b4b
    classDef backend    fill:#F0FFF4,stroke:#22C55E,color:#14532d
    classDef s3_dump    fill:#F9FAFB,stroke:#6B7280,color:#374151
    classDef s3_fail    fill:#FEF2F2,stroke:#EF4444,color:#7f1d1d
    classDef s3_ok      fill:#F0FFF4,stroke:#22C55E,color:#14532d
    classDef s3_prod    fill:#EFF6FF,stroke:#3B82F6,color:#1e3a8a
    classDef monitor    fill:#FFFBEB,stroke:#F59E0B,color:#78350f
    classDef cicd       fill:#F5F3FF,stroke:#8B5CF6,color:#3b0764
    classDef db         fill:#FFF7ED,stroke:#F97316,color:#7c2d12

    class CODE,MODEL,DATA input
    class BEK,MLFLOW,CI_SEC backend
    class DUMP s3_dump
    class FAIL s3_fail
    class OK s3_ok
    class PROD_STORE s3_prod
    class MONITOR monitor
    class CI_PROD cicd
    class DB_SVC,DB_ML db
```

---

## Компоненты системы

### Входные артефакты

| Компонент | Описание |
|-----------|----------|
| **Код** | Исходный код моделей и пайплайнов |
| **Модель** | Обученные ML-модели |
| **Данные** | Датасеты для обучения и валидации |

### Основные сервисы

| Компонент | Описание |
|-----------|----------|
| **Веб-интерфейс / Фронт** | UI для работы с платформой; принимает входные артефакты |
| **Бэк** | Центральный сервис; отображает все загрузки из MLflow, связан с БД сервиса |
| **MLflow** | Трекинг экспериментов и версионирование моделей/данных; требует добавления регистрации |
| **БД нашего сервиса** | Оперативная база данных бэкенда |
| **База данных MLflow** | Метаданные MLflow (поддерживается автоматически) |

> ⚠️ **Единый аккаунт**: бэк и MLflow работают под одним аккаунтом — это упрощает интеграцию, но требует учёта при разграничении прав доступа.

### Безопасность и CI/CD

| Компонент | Описание |
|-----------|----------|
| **CI/CD Security Check** | Проверка безопасности артефактов перед продвижением |
| **CI/CD прода** | Пайплайн деплоя на продакшн |
| **Мониторинг** | Защита от DDoS, контроль за API и метриками, предотвращение кражи моделей |

### Хранилище S3 (модели, данные, код)

| Бакет | Цвет | Описание |
|-------|------|----------|
| **Свалка** | ⬜ серый | Сырые/необработанные загрузки |
| **Не прошли проверку** | 🔴 красный | Артефакты, отклонённые security check |
| **OK** | 🟢 зелёный | Проверенные артефакты, готовые к использованию |
| **!ПРОД!** | 🔵 синий | Задеплоенные артефакты на продакшне |

---

## Потоки данных

### Поток загрузки артефактов
```
Датасайнтист → MLflow → Бэк (отображение) → S3 (Свалка / Не прошли / OK / ПРОД)
```

### Поток CI/CD
```
Бэк → CI/CD Security Check ↔ MLflow → CI/CD прода → S3 (!ПРОД!) + Мониторинг
```

### Поток пользователя
```
Входы (Код/Модель/Данные) → Веб-интерфейс → Бэк → MLflow
```

---

## Заметки для агентов

- `mlflow` — единая точка версионирования; **регистрация ещё не добавлена** (TODO)
- Бэк и MLflow используют **один аккаунт** — учитывать при настройке IAM/RBAC
- S3 разделён на 4 логические зоны по статусу проверки
- Мониторинг охватывает **API + метрики + защиту модели**
- CI/CD прода triggered из бэка, итоговый артефакт попадает в `S3:!ПРОД!`
