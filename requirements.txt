# Корневые зависимости платформы (бэкенд/ядро/UI).
# У КАЖДОГО ГЕЙТА — свой requirements.txt в его папке (изоляция образов).
# Версии пиннятся при реализации (require-hashes/lock); ниже — ориентир.

fastapi
uvicorn[standard]
pydantic>=2
psycopg[binary]
boto3
redis
mlflow
pandas
requests
python-dotenv

# ML (модель №1 — табличная; экспорт в ONNX = G4-разрешённый формат)
scikit-learn
skl2onnx
onnx
onnxruntime
numpy

# UI
streamlit

# Тесты
pytest
