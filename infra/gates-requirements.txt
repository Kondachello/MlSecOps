# Зависимости gates-runner-образа (см. infra/Dockerfile.gates).
# Держим минимальный набор, без mlsec_app/streamlit/fastapi — гейтам это не нужно.
mlflow==2.17.2
onnxruntime==1.20.1
numpy>=1.26,<2.3
pandas>=2.1,<2.3
scikit-learn>=1.4,<1.6
pyyaml>=6.0
boto3>=1.34
requests>=2.31
