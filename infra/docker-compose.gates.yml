# Образы гейтов (G1–G5) собираются одной командой из корня репо:
#
#   bash scripts/ci/build_all_gates.sh
#
# Отдельные сервисы здесь не нужны — гейты в CI пока запускаются через python,
# Docker-образы понадобятся на этапе 1 (job build-gates).
